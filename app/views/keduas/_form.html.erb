<%= form_with(model: kedua, local: true) do |form| %>
  <% if kedua.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kedua.errors.count, "error") %> prohibited this kedua from being saved:</h2>

      <ul>
      <% kedua.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nama %>
    <%= form.text_field :nama, id: :kedua_nama %>
  </div>

  <div class="field">
    <%= form.label :alamat %>
    <%= form.text_area :alamat, id: :kedua_alamat %>
  </div>

  <div class="field">
    <%= form.label :no_hp %>
    <%= form.number_field :no_hp, id: :kedua_no_hp %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
